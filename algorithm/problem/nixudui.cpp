#include<bits/stdc++.h>

using namespace std;

const int N = 1000000006;

int nums[N];

void sovle(int time,int *nums){
    
}

int main(){
    int n;
    for(int i = 0 ; i < pow(2,n) ; i ++){
        cin >> nums[i];
    }
    int m ;
    cin >>m;
    while(m--){
        cin >> n;
        sovle(n,nums);
    }
    return 0;
}